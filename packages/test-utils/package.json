{
  "name": "@llmtoolbox/test-utils",
  "version": "0.1.1",
  "description": "Shared test utilities for LLM Toolbox",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist",
    "src"
  ],
  "scripts": {
    "build": "tsc -b",
    "clean": "rm -rf dist",
    "lint": "eslint src/**/*.ts"
  },
  "dependencies": {
    "vitest": "^1.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.1.4",
    "typescript": "^5.0.4"
  }
}